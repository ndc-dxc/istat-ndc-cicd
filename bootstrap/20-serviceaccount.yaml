# The identity every PipelineRun runs as. It lives in the control namespace and
# is granted rights on the environment namespaces, never cluster-wide.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: deployer
  namespace: istat-ndc-cicd
